version: "3.8"

services:

  backend:
    build:
      context: ./backend
    container_name: fastapi-backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://postgres.oxrnnsapiahpfytbpljc:KRuchika915@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres
      SECRET_KEY: collegeprojectsecretkey123
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 30
    restart: always


  frontend:
    build:
      context: ./frontend
    container_name: react-frontend
    ports:
      - "5175:80"
    depends_on:
      - backend
    restart: always